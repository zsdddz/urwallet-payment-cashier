---
description: 要求 git commit 信息使用中文
alwaysApply: true
---

# Git Commit 信息规范

创建 git commit 时，**必须使用中文**编写 commit 消息。

## 格式要求

- **标题行**：简明扼要描述改动（不超过 50 字）
- **正文**（可选）：详细说明改动原因和内容

## 示例

```
feat: 新增用户登录功能

- 添加手机号验证码登录
- 支持记住登录状态
```

```
fix: 修复支付金额精度问题
```

```
refactor: 重构收银台页面组件结构
```

## 类型前缀（中英混用时保留英文前缀）

- `feat:` 新功能
- `fix:` 修复问题
- `refactor:` 代码重构
- `style:` 样式调整
- `docs:` 文档更新
- `chore:` 构建/配置变更
- `test:` 测试相关
